version: 0.0.1
icon: ./icon.png
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    set -e
    sudo apt-get update
    pip install --upgrade pip
    # pip install -U "magic-pdf[full]" --extra-index-url https://wheels.myhloli.com -i https://pypi.tuna.tsinghua.edu.cn/simple --no-cache-dir
    # pip install -r requirements.txt